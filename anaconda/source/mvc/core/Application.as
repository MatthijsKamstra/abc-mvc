package mvc.core {	import flash.display.MovieClip;	import flash.display.StageAlign;	import flash.display.StageScaleMode;	import flash.events.Event;	public class Application extends MovieClip {		private var _flashVars : Object = null;		public function Application() {				// Check stage and call initialize.			if (stage) 				initialize(null); 			else  				addEventListener(Event.ADDED_TO_STAGE, initialize);		}		private function initialize(e : Event) : void {			removeEventListener(Event.ADDED_TO_STAGE, initialize);			// get flashvars from html document 			flashVars = stage.loaderInfo.parameters;			// start init (collection of startup commands)			init();		}		protected function init() : void {			setupStage();			setupApplication();		}		protected function setupStage() : void {
			stage.scaleMode = StageScaleMode.NO_SCALE;			stage.align = StageAlign.TOP_LEFT;		}		protected function setupApplication() : void {			trace("override protected function setupApplication() : void {}");			// This is where you setup the application.		}		public function get flashVars() : Object {			return _flashVars;		}		public function set flashVars( obj : Object ) : void {			_flashVars = obj;		}	}}